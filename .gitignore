.env
.idea/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
node_modules/
front/dist/
front/.vite/
uploads/
*.log
src/data/runtime_kv.json

# Tauri updater signing key (NEVER commit)
*.tauri.key
.tauri/

# PyInstaller build artifacts (should never be committed)
build_pyinstaller/
dist_pyinstaller/
*.spec

# Tauri / Rust build artifacts
src-tauri/target/
# sidecar 二进制（由 build_backend.ps1 重新生成，体积巨大，不入库）
src-tauri/binaries/*.exe

# 干净打包用的虚拟环境
.venv-build/
